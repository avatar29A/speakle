<view:BaseUserControlView x:Class="Hqub.Speckle.GUI.View.Shell.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:view="clr-namespace:Hqub.Speckle.GUI.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Menu>
            <MenuItem Header="Файл">
                <MenuItem Header="Новый эксперимент"></MenuItem>
                <Separator/>
                <MenuItem Header="Выход"></MenuItem>
            </MenuItem>
            <MenuItem Header="?">
                <MenuItem Header="Настройки"/>
                <MenuItem Header="Помощь"/>
            </MenuItem>
        </Menu>
        
        <DockPanel Grid.Row="1"
                   LastChildFill="True"
                   >
            <ToolBar DockPanel.Dock="Right">
                <StackPanel Orientation="Horizontal" Grid.Column="1"
                        HorizontalAlignment="Right"
                        >
                    <ToggleButton Width="24" 
                        Height="24" 
                        Content="2x2" 
                        Tag="2" 
                        ToolTip="Включить сетку 2 на 2"
                        Click="ChangeGridSize_OnClick"/>
                </StackPanel>
            </ToolBar>
            
            <ToolBar Grid.Row="1" HorizontalAlignment="Stretch">
               
            </ToolBar>
            
            
        </DockPanel>
        
        

        <Grid Grid.Row="2">
           <Grid.ColumnDefinitions>
               <ColumnDefinition Width="Auto"/>
               <ColumnDefinition/>
           </Grid.ColumnDefinitions>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            
            <ListBox Width="200"
                     Grid.Row="1"
                     
                     ></ListBox>
          
            <Grid x:Name="ContentGrid" Grid.Column="1" 
                  Grid.Row="1"
                  ShowGridLines="True">
            </Grid>
        </Grid>
    </Grid>
</view:BaseUserControlView>
